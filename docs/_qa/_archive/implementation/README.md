# Archived Implementation QA

This folder is reserved for superseded implementation QA artifacts that no longer belong in `docs/_qa/implementation/`.

At the moment, the earlier implementation QA artifacts remain in the active path because they still document the executable baseline and the approved implementation chain that the repo references today.
