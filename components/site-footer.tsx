export function SiteFooter() {
  return (
    <footer className="panel-shell panel-neutral mt-8 px-5 py-6 text-(--ink-body)">
      <p className="type-concept text-(--ink-strong)">
        Start with the guided build. Use browse and examples only when they sharpen the next move.
      </p>
      <p className="type-body measure-reading mt-2">
        Start with the message, choose the signal, pick the visual direction, show the proof,
        build the page, and put it in public.
      </p>
    </footer>
  );
}
